# get_item_index
Finds one of the children of this node by its ID.

`int get_item_index(const string &in id) const;`

## Arguments:
- `const string &in id`: The ID to look for.

## Returns:
`int`: The zero based index into `nodes`, or -1 when nothing matches.
