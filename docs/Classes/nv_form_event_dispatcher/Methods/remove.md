# remove
Unregisters a previously added event handler.

`bool remove(nv_form_event_handler@ cb);`

## Arguments:
- `nv_form_event_handler@ cb`: The handler to remove.

## Returns:
`bool`: True if the handler was registered and has been removed, false otherwise.
