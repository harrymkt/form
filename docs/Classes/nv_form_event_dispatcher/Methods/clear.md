# clear
Unregisters every event handler at once.

`void clear();`

## Remarks:
This also drops the default event handler that the control inherited from `nv_form.default_event_handler` when it was created.
