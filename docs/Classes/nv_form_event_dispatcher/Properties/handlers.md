# handlers
The array of registered event handlers, in the order they will be called.

`nv_form_event_handler@[] handlers;`

## Remarks:
Exposed so you can inspect what is registered. Prefer `add`, `remove` and `clear` over modifying this array directly.
