# character
The character (if any).

`string character;`

## Remarks:
When you receive an event and if this value is not empty, check the properties like `deleted`. For example, if `deleted` returns `false`, then this character is typed. This is useful to play keyboard sounds as type etc.
