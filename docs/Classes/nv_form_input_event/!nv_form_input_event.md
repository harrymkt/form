# nv_form_input_event
An input event, child class of the `nv_form_event`.

`nv_form_input_event(nv_form_control@ ctrl);`

## Arguments:
- `nv_form_control@ ctrl`: A handle to the control the event is dispatched from.
