# monitor
Runs one iteration of the menu's own input loop.

`void monitor();`

## Remarks:
This is the `nv_utility_form` implementation. The parent form calls it every frame while the menu is open, you should not call it yourself.
