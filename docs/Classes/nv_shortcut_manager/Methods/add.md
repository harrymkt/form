# add
Adds a shortcut to the manager

`void add(nv_shortcut@ s);`

## Arguments:
- `nv_shortcut@ s`: A handle to the shortcut you want to add.

## Remarks:
This is equivalent to using `manager.shortcuts.insert_last(handle);`.
