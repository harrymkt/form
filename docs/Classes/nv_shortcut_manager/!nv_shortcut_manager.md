# nv_shortcut_manager
This class allows the managing of custom shortcuts.

## Remarks:
This class internally uses engine's key event handler to handle and detect keypresses. It is thus this class is responsible for managing keypresses, taps, and all shortcut related actions.
