# suggestions
The pool of every completion this manager can offer.

`string[] suggestions;`

## Remarks:
Assign to this array directly to replace the whole pool, or use `add_suggestion` and `add_suggestions` to append to it.
