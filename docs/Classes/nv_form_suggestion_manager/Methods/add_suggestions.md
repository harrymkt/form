# add_suggestions
Adds several entries to the pool of possible completions at once.

`void add_suggestions(const string[] &in s);`

## Arguments:
- `const string[] &in s`: The texts to offer.

## Remarks:
Appends to whatever is already in the pool, it does not replace it.
